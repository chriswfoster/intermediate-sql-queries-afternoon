/* BLACK DIAMOND!!!!!!!!! */

/* SELECT T.Name, Genre.name, Album.Title, Artist.Name 
FROM Playlist
JOIN PlaylistTrack ON PlaylistTrack.PlaylistId = Playlist.PlaylistId
JOIN Track T ON T.TrackId = PlaylistTrack.TrackId
JOIN Album ON Album.AlbumID = T.AlbumId
JOIN Artist ON Artist.ArtistID = Album.ArtistID
JOIN Genre ON T.GenreId = Genre.GenreId
WHERE Playlist.Name = "Music" */
